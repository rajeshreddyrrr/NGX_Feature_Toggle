import { EnableFeatureDirective } from './enable-feature.directive';

describe('EnableFeatureDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableFeatureDirective();
    expect(directive).toBeTruthy();
  });
});
